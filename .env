# Claude Code Command Configuration
CLAUDE_CODE_COMMAND=claude
CLAUDE_CODE_TIMEOUT=300

# Logging Configuration
LOG_LEVEL=INFO

# Database Configuration (optional, defaults to sqlite in project root)
# DATABASE_PATH=/path/to/custom/database.db

# Workspace Configuration (optional, defaults to workspace/ in project root)
# WORKSPACE_DIR=/path/to/custom/workspace